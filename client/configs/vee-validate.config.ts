const veeValidate = {
  autoImports: true, // disable or enable auto imports
  // Use different names for components
  componentNames: {
    Form: "VeeForm",
    Field: "VeeField",
    FieldArray: "VeeFieldArray",
    ErrorMessage: "VeeErrorMessage"
  }
}

export default veeValidate
